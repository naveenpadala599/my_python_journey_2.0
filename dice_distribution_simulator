import random
x = []
l = int(input("Enter the number of sides of dice: "))
for i in range(0, l-1):
    x.append(i+1)
count = []*l
s = int(input("Enter the sample size"))
for j in range(s):
    y = random.choice(x)
    count[y-1]+=1
for i in range(x):
    print(f"Number of x[i]'s is count[i]")
