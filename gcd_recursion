a = int(input('Enter the first number: '))
b = int(input('Enter the second number: '))
def greatest_divisor(x,y):
    if y==0:
        return x
    else:
        return(greatest_divisor(y,x%y))
print(greatest_divisor(a,b))    
    
    
