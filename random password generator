import random
import string
print(f'''Your random password is "{"".join(random.choice(string.ascii_letters+string.digits+string.punctuation
)for i in range(int(input("Enter the length of password you want: "))
))}"''')    
